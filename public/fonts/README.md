# Self-hosted fonts

Place these font files here for fully offline typography:

- `Inter-Variable.woff2`
- `SpaceGrotesk-Variable.woff2`
- `JetBrainsMono-Variable.woff2`

The app falls back to local/system fonts when these files are absent.
